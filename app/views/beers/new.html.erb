<h1>New beer</h1>

<%= form_for(@beer) do |f| %>
  brewery: <%= f.select :brewery_id, options_from_collection_for_select(@breweries, :id, :name) %>
  name: <%= f.text_field :name %>
  style: <%= f.select :style, @styles %>

  <%= f.submit %>
<% end %>

<br>

<div>
  <%= link_to "Back to beers", beers_path %>
</div>
